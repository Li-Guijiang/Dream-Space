# Dream-Space 梦境空间 · 部署指南

> 这是一份给「新手」看的部署指南。你现在的进度是：

---

## 一、先搞懂三个概念（很重要，读完就明白了）

| 概念 | 你已经有的 / 缺的 | 打个比方 |
|:-----|:-----|:-----|
| **域名** | ✅ 已买 | 「门牌号 / 地址」，它本身**不能放东西** |
| **服务器**（ECS / 轻量服务器） | ❌ 还没买 | 「房子」，网站代码和数据库都住在这里 |
| **备案**（ICP 备案） | ❌ 还没做 | 「房产证」，大陆地区必须办，否则网站会被拦截 |

**一句话总结现状：你有了门牌号，但还没有房子。没房子，网站就没地方住，所以现在确实无法真正「部署上线」。**

**只买域名、没买服务器，后端用不了**——这是对的。因为你的项目是一个「全栈项目」，需要下面三样东西同时在一台服务器上运行：

- **前端**：Next.js（需要 Node.js）
- **后端**：FastAPI（需要 Python）
- **数据库**：PostgreSQL

---

## 二、「用我电脑当服务器」为什么不可行？

这不是我不愿意，而是技术上在公网基本行不通：

1. **家庭宽带大多没有公网 IP**（运营商做了 NAT），外网根本访问不到你的电脑；
2. 即使有公网 IP，**80 / 443 端口通常被运营商封禁**，网站无法通过域名访问；
3. **备案要求接入商是云服务商**，用自己电脑无法备案；
4. 电脑必须 **24 小时开机、不断网**，一关机网站就没了。

所以结论是：**等买了阿里云服务器再正式部署**。现在阶段（已经可以做的）是：

- ✅ 代码上传 GitHub（**已完成**）
- ✅ 本地把网站跑起来看看效果（见「九、本地预览」）
- ✅ 把部署方案准备好（就是本文 + `deploy/` 目录下的配置）

---

## 三、买什么服务器？

- 推荐：**阿里云「轻量应用服务器」**（比 ECS 便宜、有图形界面，新手友好）
- 配置：**2 核 2G 起步**，最好 2 核 4G（这个项目前端 Next.js + 后端 + 数据库，内存会吃紧）
- 地域：**选中国大陆**（杭州 / 北京等），这样才符合备案要求
- 系统：**Ubuntu 20.04 / 22.04** 或 **Debian**，方便装宝塔面板

---

## 四、备案流程（买完服务器立刻做，周期约 1~2 周）

> 关键点：**备案是绑定在服务器上的**，所以必须先有服务器，才能提交备案。

1. 登录阿里云控制台 → 搜索「备案」进入备案专区；
2. 填写主体信息（个人）+ 网站信息，域名填 `dream-space.vip`，关联你买的服务器；
3. 阿里云初审 → 提交到管局 → 管局审核（约 1~2 周）；
4. 审核通过后，你会拿到一个备案号（形如 `赣ICP备XXXXXXXX号`）；
5. 把备案号填到 `Kirameku/siteConfig.ts` 里的 `icpConfig.name`（我已经帮你留空待填）。

> ⚠️ 备案期间网站不能访问（需要「关站」），等备案通过再正式对外。这个项目底部原本展示的是原作者备案号，我已清空，等你自己的备案号下来再填。

---

## 五、正式部署（推荐路线：宝塔面板）

> 这个项目的原作者（Starhiro）就是用阿里云 + 宝塔面板部署的，踩坑记录在 `DEPLOY_NOTES.md`，我把它整理成了下面的完整步骤。

### 步骤 1：服务器安装宝塔面板

买好服务器后，SSH 登录，执行宝塔安装命令（以 Ubuntu 为例）：

```bash
wget -O install.sh https://download.bt.cn/install/install-ubuntu_6.0.sh && bash install.sh
```

装完后浏览器访问面板地址，登录后它会提示你安装运行环境。

### 步骤 2：安装运行环境

在宝塔面板「软件商店」安装：

- **Nginx**（最新版即可）
- **PostgreSQL 管理器**（或直接用 Docker 装一个 postgres:16）
- **Node.js 版本管理器**（选 Node 20 或 22）
- **Python 项目管理器**（选 Python 3.11 / 3.12）

### 步骤 3：上传项目代码

把 `Kirameku`（前端）和 `Kirameku-backend`（后端）两个文件夹上传到服务器，例如放到 `/www/wwwroot/dream-space/`。

### 步骤 4：配置并启动后端

```bash
cd Kirameku-backend
python3 -m venv venv
source venv/bin/activate          # 激活虚拟环境
pip install -r requirements.txt   # 安装依赖

cp .env.example .env              # 复制并编辑 .env
# 重点改这几项：
#   DATABASE_URL=postgresql://用户名:密码@127.0.0.1:5432/数据库名
#   SECRET_KEY=随便一长串随机字符
#   CORS_ORIGINS=https://dream-space.vip
#   OSS_*（如果要用阿里云 OSS 存图片，填你自己的；暂时不用可先不填）

# 初始化数据库（先建好数据库，再执行）
psql -U postgres -d 你的数据库名 -f init_db.sql

# 打包管理后台（Vue → dist）
cd admin && npm install && npm run build && cd ..

# 启动后端（端口 8000）
uvicorn app.main:app --host 0.0.0.0 --port 8000
```

验证：访问 `http://服务器IP:8000/docs` 能看到 API 文档即成功。

### 步骤 5：配置并启动前端

```bash
cd Kirameku
npm install
npm run build       # 打包
npm run start       # 生产模式启动，端口 3000
```

> 小内存服务器可加环境变量限制内存：`NODE_OPTIONS=--max-old-space-size=512 npm run start`

### 步骤 6：配置 Nginx 反向代理

把 `deploy/nginx.conf` 的内容复制到宝塔「网站 → 对应站点 → 配置文件」，然后重载 Nginx。核心逻辑：

- `/` → 转发给前端 `127.0.0.1:3000`
- `/api/` → 转发给后端 `127.0.0.1:8000`
- `/uploads/` → 转发给后端 `127.0.0.1:8000`

### 步骤 7：解析域名 + 上线

1. 阿里云控制台 → 域名解析 → 给 `dream-space.vip` 和 `www.dream-space.vip` 添加 **A 记录**，指向服务器公网 IP；
2. 备案通过后，网站即可通过 `https://dream-space.vip` 访问；
3. 在宝塔面板给站点申请免费 SSL 证书（Let's Encrypt），启用 HTTPS。

---

## 六、Docker 一键部署（可选，进阶）

我已经在 `deploy/` 目录准备好了 `docker-compose.yml` 和两个 Dockerfile，服务器装了 Docker 后，在项目根目录执行：

```bash
docker compose -f deploy/docker-compose.yml up -d --build
```

会一次性启动 postgres + 后端 + 前端 + nginx 四个容器。**注意**：这是标准配置的起点，首次使用可能需要根据你的实际情况微调（例如数据库密码、环境变量）。小内存服务器建议用上面宝塔路线，因为管理后台打包需要较大内存。

---

## 七、上线后需要你亲自改的几处（重要）

代码里还有几处**原作者的信息**，上线前请改成你自己的（都在 `Kirameku/siteConfig.ts`）：

- `authorName`、`bio` —— 作者名和简介
- `avatarUrl` —— 头像图片
- `social` 里的 `gitee` / `google` / `email` / `qq` / `wechat` —— 社交链接
- `cloudMusicPlaylistId` —— 网易云歌单（可选）
- `icpConfig.name` —— 你自己的备案号

---

## 八、常见坑（来自原作者 DEPLOY_NOTES.md）

1. **Nginx 一定要禁用缓存**，否则每次重新打包后浏览器会加载旧的 HTML 页面（`deploy/nginx.conf` 里已经加好了 `proxy_no_cache` 等配置）；
2. **小内存服务器**（1.8G）跑 Next.js 会吃力，务必加 `NODE_OPTIONS=--max-old-space-size=512`；
3. 部署后可用 `curl -s https://你的域名/ | grep buildId` 确认返回的是最新构建。

---

## 九、本地预览（现在就能做，看看效果）

在你自己电脑上（不需要服务器），打开终端依次执行：

```bash
# 后端（需要本地装好 PostgreSQL，没装可以先跳过，只预览前端界面）
cd Kirameku-backend
python -m venv venv && source venv/bin/activate
pip install -r requirements.txt
uvicorn app.main:app --host 0.0.0.0 --port 8000

# 前端（另开一个终端）
cd Kirameku
npm install
npm run dev       # 浏览器打开 http://localhost:3000
```

> 说明：没有后端时，首页的动态内容（文章、说说等）是空的，但能看到完整界面和 UI 效果。
